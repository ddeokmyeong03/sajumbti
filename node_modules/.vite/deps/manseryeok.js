//#region node_modules/manseryeok/dist/index.js
/**
* 만세력(萬歲曆) 계산 라이브러리
* Korean Saju (Four Pillars) and Manseryeok calculation library
*
* @author Yoohyojun
* @license MIT
*/
/** 천간 (Heavenly Stems) */
var HEAVENLY_STEMS = [
	"갑",
	"을",
	"병",
	"정",
	"무",
	"기",
	"경",
	"신",
	"임",
	"계"
];
/** 천간 한자 */
var HEAVENLY_STEMS_HANJA = [
	"甲",
	"乙",
	"丙",
	"丁",
	"戊",
	"己",
	"庚",
	"辛",
	"壬",
	"癸"
];
/** 지지 (Earthly Branches) */
var EARTHLY_BRANCHES = [
	"자",
	"축",
	"인",
	"묘",
	"진",
	"사",
	"오",
	"미",
	"신",
	"유",
	"술",
	"해"
];
/** 지지 한자 */
var EARTHLY_BRANCHES_HANJA = [
	"子",
	"丑",
	"寅",
	"卯",
	"辰",
	"巳",
	"午",
	"未",
	"申",
	"酉",
	"戌",
	"亥"
];
/** 음양 (Yin/Yang) */
var YIN_YANG = ["양", "음"];
/** 오행 (Five Elements) */
var FIVE_ELEMENTS = [
	"목",
	"화",
	"토",
	"금",
	"수"
];
/** 1900-2100년 음력 데이터 (출처: 한국천문연구원) */
var LUNAR_DATA = [
	19416,
	19168,
	42352,
	21717,
	53856,
	55632,
	91476,
	22176,
	39632,
	21970,
	19168,
	42422,
	42192,
	53840,
	119381,
	46400,
	54944,
	44450,
	38320,
	84343,
	18800,
	42160,
	46261,
	27216,
	27968,
	109396,
	11104,
	38256,
	21234,
	18800,
	25958,
	54432,
	59984,
	28309,
	23248,
	11104,
	100067,
	37600,
	116951,
	51536,
	54432,
	120998,
	46416,
	22176,
	107956,
	9680,
	37584,
	53938,
	43344,
	46423,
	27808,
	46416,
	86869,
	19872,
	42416,
	83315,
	21168,
	43432,
	59728,
	27296,
	44710,
	43856,
	19296,
	43748,
	42352,
	21088,
	62051,
	55632,
	23383,
	22176,
	38608,
	19925,
	19152,
	42192,
	54484,
	53840,
	54616,
	46400,
	46752,
	103846,
	38320,
	18864,
	43380,
	42160,
	45690,
	27216,
	27968,
	44870,
	43872,
	38256,
	19189,
	18800,
	25776,
	29859,
	59984,
	27480,
	21952,
	43872,
	38613,
	37600,
	51552,
	55636,
	54432,
	55888,
	30034,
	22176,
	43959,
	9680,
	37584,
	51893,
	43344,
	46240,
	47780,
	44368,
	21977,
	19360,
	42416,
	86390,
	21168,
	43312,
	31060,
	27296,
	44368,
	23378,
	19296,
	42726,
	42208,
	53856,
	60005,
	54576,
	23200,
	30371,
	38608,
	19195,
	19152,
	42192,
	118966,
	53840,
	54560,
	56645,
	46496,
	22224,
	21938,
	18864,
	42359,
	42160,
	43600,
	111189,
	27936,
	44448,
	84835,
	37744,
	18936,
	18800,
	25776,
	92326,
	59984,
	27424,
	108228,
	43744,
	41696,
	53987,
	51552,
	54615,
	54432,
	55888,
	23893,
	22176,
	42704,
	21972,
	21200,
	43448,
	43344,
	46240,
	46758,
	44368,
	21920,
	43940,
	42416,
	21168,
	45683,
	26928,
	29495,
	27296,
	44368,
	84821,
	19296,
	42352,
	21732,
	53600,
	59752,
	54560,
	55968,
	92838,
	22224,
	19168,
	43476,
	41680,
	53584,
	62034,
	54560
];
/** 음력 연도의 총 일수를 계산 */
var getLunarYearDays = (year) => {
	let sum = 348;
	for (let i = 32768; i > 8; i >>= 1) sum += LUNAR_DATA[year - 1900] & i ? 1 : 0;
	return sum + getLeapMonthDays(year);
};
/** 음력 연도의 윤달 위치를 반환 (0이면 윤달 없음) */
var getLeapMonth = (year) => LUNAR_DATA[year - 1900] & 15;
/** 음력 연도의 윤달 일수를 반환 */
var getLeapMonthDays = (year) => {
	if (getLeapMonth(year)) return LUNAR_DATA[year - 1900] & 65536 ? 30 : 29;
	return 0;
};
/** 음력 특정 월의 일수를 반환 */
var getLunarMonthDays = (year, month) => LUNAR_DATA[year - 1900] & 65536 >> month ? 30 : 29;
/**
* 음력을 양력으로 변환
*/
function lunarToSolar(year, month, day, isLeapMonth) {
	const baseDate = new Date(1900, 0, 31);
	let offset = 0;
	for (let i = 1900; i < year; i++) offset += getLunarYearDays(i);
	const leapMonth = getLeapMonth(year);
	let isLeap = false;
	for (let i = 1; i < month; i++) if (leapMonth > 0 && i === leapMonth && !isLeap) {
		offset += getLeapMonthDays(year);
		isLeap = true;
		i--;
	} else offset += getLunarMonthDays(year, i);
	if (isLeapMonth && leapMonth === month) offset += getLunarMonthDays(year, month);
	offset += day - 1;
	const solarDate = new Date(baseDate.getTime() + offset * 864e5);
	return {
		year: solarDate.getFullYear(),
		month: solarDate.getMonth() + 1,
		day: solarDate.getDate()
	};
}
/**
* 양력을 음력으로 변환
*/
function solarToLunar(year, month, day) {
	const baseDate = new Date(1900, 0, 31);
	const targetDate = new Date(year, month - 1, day);
	const offset = Math.floor((targetDate.getTime() - baseDate.getTime()) / 864e5);
	let lunarYear = 1900;
	let remainingDays = offset;
	for (let i = 1900; i < 2100 && remainingDays > 0; i++) {
		const yearDays = getLunarYearDays(i);
		if (remainingDays < yearDays) {
			lunarYear = i;
			break;
		}
		remainingDays -= yearDays;
	}
	const leapMonth = getLeapMonth(lunarYear);
	let lunarMonth = 1;
	let isLeapMonth = false;
	for (let i = 1; i <= 12 && remainingDays > 0; i++) {
		let monthDays;
		if (leapMonth > 0 && i === leapMonth + 1 && !isLeapMonth) {
			monthDays = getLeapMonthDays(lunarYear);
			isLeapMonth = true;
			i--;
		} else {
			monthDays = getLunarMonthDays(lunarYear, i);
			isLeapMonth = false;
		}
		if (remainingDays < monthDays) {
			lunarMonth = i;
			break;
		}
		remainingDays -= monthDays;
	}
	const lunarDay = remainingDays + 1;
	return {
		year: lunarYear,
		month: lunarMonth,
		day: lunarDay,
		isLeapMonth
	};
}
/** 절기 계산을 위한 기본 데이터 */
var SOLAR_TERM_BASE = [
	5.4055,
	20.12,
	3.87,
	18.73,
	5.63,
	20.646,
	4.81,
	20.1,
	5.52,
	21.04,
	5.678,
	21.37,
	7.108,
	22.83,
	7.5,
	23.13,
	7.646,
	23.042,
	8.318,
	23.438,
	7.438,
	22.36,
	7.18,
	21.94
];
/**
* 특정 절기의 날짜를 계산
* @param year 연도
* @param termIndex 절기 인덱스 (0-23)
*/
var getSolarTermDate = (year, termIndex) => {
	const century = Math.floor(year / 100);
	const yearInCentury = year % 100;
	const termCoeff = .2422;
	const leapYearAdjust = Math.floor(yearInCentury / 4) - Math.floor(century / 4);
	const day = Math.floor(SOLAR_TERM_BASE[termIndex] + termCoeff * yearInCentury + leapYearAdjust);
	const month = Math.floor(termIndex / 2);
	return new Date(year, month, day);
};
/**
* 연주 계산
*/
var getYearPillar = (year) => ({
	heavenlyStem: HEAVENLY_STEMS[(year - 4) % 10],
	earthlyBranch: EARTHLY_BRANCHES[(year - 4) % 12]
});
/**
* 월주 계산 (절기 기준)
*/
var getMonthPillar = (year, month, day) => {
	const date = new Date(year, month - 1, day);
	const lichunDate = getSolarTermDate(year, 2);
	let adjustedYear = year;
	if (date < lichunDate) adjustedYear = year - 1;
	let solarTermMonth = 0;
	for (let i = 0; i < 24; i += 2) if (date >= getSolarTermDate(adjustedYear, i)) solarTermMonth = Math.floor(i / 2) + 1;
	else break;
	return {
		heavenlyStem: HEAVENLY_STEMS[((adjustedYear - 4) % 10 % 5 * 2 + solarTermMonth + 1) % 10],
		earthlyBranch: {
			1: "인",
			2: "묘",
			3: "진",
			4: "사",
			5: "오",
			6: "미",
			7: "신",
			8: "유",
			9: "술",
			10: "해",
			11: "자",
			12: "축"
		}[solarTermMonth] || "인"
	};
};
/**
* 일주 계산 (60갑자 순환)
*/
var getDayPillar = (year, month, day) => {
	const BASE_DATE = new Date(1992, 9, 24);
	const BASE_GANJI_NUM = 9;
	const targetDate = new Date(year, month - 1, day);
	const targetGanjiNum = ((BASE_GANJI_NUM + Math.floor((targetDate.getTime() - BASE_DATE.getTime()) / 864e5)) % 60 + 60) % 60;
	return {
		heavenlyStem: HEAVENLY_STEMS[targetGanjiNum % 10],
		earthlyBranch: EARTHLY_BRANCHES[targetGanjiNum % 12]
	};
};
/**
* 시주 계산
*
* 시간 체계:
* - 자시(子時): 23:00-01:00
* - 축시(丑時): 01:00-03:00
* - 인시(寅時): 03:00-05:00
* - 묘시(卯時): 05:00-07:00
* - 진시(辰時): 07:00-09:00
* - 사시(巳時): 09:00-11:00
* - 오시(午時): 11:00-13:00
* - 미시(未時): 13:00-15:00
* - 신시(申時): 15:00-17:00
* - 유시(酉時): 17:00-19:00
* - 술시(戌時): 19:00-21:00
* - 해시(亥時): 21:00-23:00
*
* 분(minute) 처리:
* - 30분 이전: 해당 시진의 초반
* - 30분 이후: 다음 시진으로 넘어가는 경계
* - 예: 5시 30분은 묘시의 중반으로 처리
*/
var getHourPillar = (dayPillar, hour, minute) => {
	let adjustedHour = hour;
	if (hour === 23) adjustedHour = 0;
	const totalMinutes = adjustedHour * 60 + minute;
	const shichen = Math.floor((totalMinutes + 60) / 120) % 12;
	return {
		heavenlyStem: HEAVENLY_STEMS[(HEAVENLY_STEMS.indexOf(dayPillar.heavenlyStem) % 5 * 2 + shichen) % 10],
		earthlyBranch: EARTHLY_BRANCHES[shichen]
	};
};
/**
* 사주팔자를 계산합니다.
*
* @param birthInfo 생년월일시 정보
* @returns 사주팔자 (연주, 월주, 일주, 시주)
*/
function calculateFourPillars(birthInfo) {
	const { hour, minute } = birthInfo;
	let { year, month, day } = birthInfo;
	if (birthInfo.isLunar) {
		const solarDate = lunarToSolar(year, month, day, birthInfo.isLeapMonth || false);
		year = solarDate.year;
		month = solarDate.month;
		day = solarDate.day;
	}
	const yearPillar = getYearPillar(year);
	const monthPillar = getMonthPillar(year, month, day);
	const dayPillar = getDayPillar(year, month, day);
	const hourPillar = getHourPillar(dayPillar, hour, minute);
	return {
		year: yearPillar,
		month: monthPillar,
		day: dayPillar,
		hour: hourPillar,
		yearElement: {
			stem: getHeavenlyStemElement(yearPillar.heavenlyStem),
			branch: getEarthlyBranchElement(yearPillar.earthlyBranch)
		},
		monthElement: {
			stem: getHeavenlyStemElement(monthPillar.heavenlyStem),
			branch: getEarthlyBranchElement(monthPillar.earthlyBranch)
		},
		dayElement: {
			stem: getHeavenlyStemElement(dayPillar.heavenlyStem),
			branch: getEarthlyBranchElement(dayPillar.earthlyBranch)
		},
		hourElement: {
			stem: getHeavenlyStemElement(hourPillar.heavenlyStem),
			branch: getEarthlyBranchElement(hourPillar.earthlyBranch)
		},
		yearYinYang: {
			stem: getHeavenlyStemYinYang(yearPillar.heavenlyStem),
			branch: getEarthlyBranchYinYang(yearPillar.earthlyBranch)
		},
		monthYinYang: {
			stem: getHeavenlyStemYinYang(monthPillar.heavenlyStem),
			branch: getEarthlyBranchYinYang(monthPillar.earthlyBranch)
		},
		dayYinYang: {
			stem: getHeavenlyStemYinYang(dayPillar.heavenlyStem),
			branch: getEarthlyBranchYinYang(dayPillar.earthlyBranch)
		},
		hourYinYang: {
			stem: getHeavenlyStemYinYang(hourPillar.heavenlyStem),
			branch: getEarthlyBranchYinYang(hourPillar.earthlyBranch)
		},
		yearString: `${yearPillar.heavenlyStem}${yearPillar.earthlyBranch}`,
		monthString: `${monthPillar.heavenlyStem}${monthPillar.earthlyBranch}`,
		dayString: `${dayPillar.heavenlyStem}${dayPillar.earthlyBranch}`,
		hourString: `${hourPillar.heavenlyStem}${hourPillar.earthlyBranch}`,
		yearHanja: `${HEAVENLY_STEMS_HANJA[HEAVENLY_STEMS.indexOf(yearPillar.heavenlyStem)]}${EARTHLY_BRANCHES_HANJA[EARTHLY_BRANCHES.indexOf(yearPillar.earthlyBranch)]}`,
		monthHanja: `${HEAVENLY_STEMS_HANJA[HEAVENLY_STEMS.indexOf(monthPillar.heavenlyStem)]}${EARTHLY_BRANCHES_HANJA[EARTHLY_BRANCHES.indexOf(monthPillar.earthlyBranch)]}`,
		dayHanja: `${HEAVENLY_STEMS_HANJA[HEAVENLY_STEMS.indexOf(dayPillar.heavenlyStem)]}${EARTHLY_BRANCHES_HANJA[EARTHLY_BRANCHES.indexOf(dayPillar.earthlyBranch)]}`,
		hourHanja: `${HEAVENLY_STEMS_HANJA[HEAVENLY_STEMS.indexOf(hourPillar.heavenlyStem)]}${EARTHLY_BRANCHES_HANJA[EARTHLY_BRANCHES.indexOf(hourPillar.earthlyBranch)]}`,
		toString: function() {
			return `${this.yearString}년주, ${this.monthString}월주, ${this.dayString}일주, ${this.hourString}시주`;
		},
		toObject: function() {
			return {
				year: this.yearString,
				month: this.monthString,
				day: this.dayString,
				hour: this.hourString
			};
		},
		toHanjaObject: function() {
			return {
				year: {
					korean: this.yearString,
					hanja: this.yearHanja
				},
				month: {
					korean: this.monthString,
					hanja: this.monthHanja
				},
				day: {
					korean: this.dayString,
					hanja: this.dayHanja
				},
				hour: {
					korean: this.hourString,
					hanja: this.hourHanja
				}
			};
		},
		toHanjaString: function() {
			return `${this.yearHanja}年柱, ${this.monthHanja}月柱, ${this.dayHanja}日柱, ${this.hourHanja}時柱`;
		}
	};
}
/**
* 사주를 한국어 문자열로 변환합니다.
*
* @param fourPillars 사주팔자
* @returns "임신연주, 경술월주, 계유일주, 을묘시주" 형식의 문자열
*/
function fourPillarsToString(fourPillars) {
	const { year, month, day, hour } = fourPillars;
	return [
		`${year.heavenlyStem}${year.earthlyBranch}연주`,
		`${month.heavenlyStem}${month.earthlyBranch}월주`,
		`${day.heavenlyStem}${day.earthlyBranch}일주`,
		`${hour.heavenlyStem}${hour.earthlyBranch}시주`
	].join(", ");
}
/**
* 천간의 음양을 반환합니다.
*/
function getHeavenlyStemYinYang(stem) {
	return HEAVENLY_STEMS.indexOf(stem) % 2 === 0 ? "양" : "음";
}
/**
* 천간의 오행을 반환합니다.
*/
function getHeavenlyStemElement(stem) {
	return {
		갑: "목",
		을: "목",
		병: "화",
		정: "화",
		무: "토",
		기: "토",
		경: "금",
		신: "금",
		임: "수",
		계: "수"
	}[stem];
}
/**
* 지지의 오행을 반환합니다.
*/
function getEarthlyBranchElement(branch) {
	return {
		자: "수",
		해: "수",
		인: "목",
		묘: "목",
		사: "화",
		오: "화",
		진: "토",
		술: "토",
		축: "토",
		미: "토",
		신: "금",
		유: "금"
	}[branch];
}
/**
* 지지의 음양을 반환합니다.
*/
function getEarthlyBranchYinYang(branch) {
	return EARTHLY_BRANCHES.indexOf(branch) % 2 === 0 ? "양" : "음";
}
//#endregion
export { EARTHLY_BRANCHES, EARTHLY_BRANCHES_HANJA, FIVE_ELEMENTS, HEAVENLY_STEMS, HEAVENLY_STEMS_HANJA, YIN_YANG, calculateFourPillars, fourPillarsToString, getEarthlyBranchElement, getEarthlyBranchYinYang, getHeavenlyStemElement, getHeavenlyStemYinYang, lunarToSolar, solarToLunar };

//# sourceMappingURL=manseryeok.js.map